{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    // Powershell script to recursively remove all children from folder
    // Specifically used to clear out .git folder from cloned repositories
    "version": "0.1.0",

    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello"
        }
    ]
}